name: free
description: Find freelas.
publish_to: "none"
version: 1.0.0+1
environment:
  sdk: ">=2.7.0 <3.0.0"
dependencies:
  flutter:
    sdk: flutter
  rating_bar: ^0.2.0
  simple_gravatar: ^1.0.5
  url_launcher: ^5.4.10
  flutter_staggered_grid_view: ^0.3.0
  http: ^0.12.1
  flutter_icons: ^1.1.0
  device_id: ^0.2.0
  grouped_list: ^3.1.0
dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:
  uses-material-design: true
